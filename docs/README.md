# docs/

Published via GitHub Pages (Settings → Pages → Source: `main` branch, `/docs` folder).

`blocklist.txt` is written here by `threat_feeds/build.py` — it does not exist yet in this
initial commit because no sync has run. Once `ops/threat_feeds_sync.sh` runs for the first time
(see README.md at the repo root), that file appears here and becomes fetchable at this repo's
Pages URL.
